'use strict';

var m = require('../../../bower_components/mithril/mithril'),
	app = require('../../../src/simple-todo/app');

describe('app.js', function(){
	describe('> todo.Todo', function() {
		it('should have "description" set to NULL and "done" set to FALSE by default', function() {
			expect(false).toBe(true);
		});
	});
});